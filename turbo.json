{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": ["**/.env.*local"],
  "globalDotEnv": [".env"],
  "pipeline": {
    "prisma#db:setup": {
      "cache": false,
      "inputs": ["./docker-compose.yml"]
    },
    "prisma#db:run": {
      "cache": false,
      "inputs": ["./docker-compose.yml"]
    },
    "prisma#db:migrate": {
      "cache": false,
      "dependsOn": ["prisma#db:run"]
    },
    "build": {
      "dependsOn": ["^build"],
      "outputs": [".next/**", "!.next/cache/**"]
    },
    "lint": {},
    "dev": {
      "cache": false,
      "persistent": true
    }
  }
}
