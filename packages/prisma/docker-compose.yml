version: "3.6"
services:
  postgres:
    image: postgres:13
    ports:
      - "5432:5432"
    restart: always
    volumes:
      - prismadb:/var/lib/postgresql/data
    env_file:
      - ./.env

volumes:
  prismadb: