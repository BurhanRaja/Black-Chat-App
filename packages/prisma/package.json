{
  "name": "prisma",
  "version": "0.0.0",
  "main": "./index.ts",
  "types": "./index.ts",
  "license": "MIT",
  "scripts": {
    "db:migrate": "prisma migrate dev --name new",
    "db:studio": "prisma studio",
    "db:up": "docker-compose up -d",
    "db:setup": "yarn run db:up & yarn run db:migrate",
    "db:env": "bash ./addEnv.sh"
  },
  "prisma": {
    "schema": "schema.prisma"
  },
  "dependencies": {
    "@prisma/client": "^5.2.0",
    "@prisma/generator-helper": "^5.2.0",
    "prisma": "^5.2.0",
    "zod": "^3.22.2",
    "zod-prisma": "^0.5.4"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
